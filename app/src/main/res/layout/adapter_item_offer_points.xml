<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:id="@+id/points_view"
             android:layout_width="match_parent"
             android:layout_height="wrap_content"
             android:background="@android:color/white"
    >

    <ImageView
        android:src="@drawable/ic_points_earn_badge"
        android:layout_width="@dimen/dtlOfferCoverImageWidth"
        android:layout_height="@dimen/dtlOfferCoverImageHeight"
        android:layout_gravity="center_vertical"
        android:layout_margin="@dimen/spacing_tiny"/>

    <TextView
        android:id="@+id/pointsDescription"
        style="@style/DreamMerchantHeaderTextAppearance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="90dp"
        android:maxLines="2"
        tools:text="Earn DreamTrips points"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_gravity="bottom"
        android:background="@drawable/divider_list"
        />
</FrameLayout>
