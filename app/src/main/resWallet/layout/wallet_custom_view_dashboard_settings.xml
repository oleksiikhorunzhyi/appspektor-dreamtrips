<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      android:layout_width="?attr/actionBarSize"
      android:layout_height="?attr/actionBarSize"
      >

   <ImageView
         android:id="@+id/icon"
         android:layout_width="match_parent"
         android:layout_height="match_parent"
         android:layout_marginBottom="16dp"
         android:layout_marginEnd="24dp"
         android:layout_marginTop="16dp"
         android:background="?attr/selectableItemBackgroundBorderless"
         android:contentDescription="@string/wallet_dashboard_menu_settings_item"
         android:src="@drawable/ic_wallet_vector_settings"
         />

   <com.worldventures.core.ui.view.custom.BadgeView
         android:id="@+id/badge_view"
         android:layout_width="16dp"
         android:layout_height="16dp"
         android:layout_alignEnd="@+id/icon"
         android:layout_alignTop="@+id/icon"
         android:layout_marginEnd="-4dp"
         android:layout_marginTop="-4dp"
         android:gravity="center"
         android:text="1"
         android:textSize="10sp"
         android:visibility="gone"
         app:background_color="@android:color/holo_red_dark"
         tools:ignore="HardcodedText,SmallSp"
         tools:visibility="gone"
         />
</RelativeLayout>