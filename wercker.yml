box: man4ik/android
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - script:
        name: show base information
        code: |
          android list targets
          android list sdk --all --extended
          gradle -v
          echo $ANDROID_HOME
          echo $ANDROID_BUILD_TOOLS
          echo $ANDROID_UPDATE_FILTER
    - setup-android-emulator:		
        target: 1
    - script:
        name: run gradle connectedCheck
        code: |
         gradle build connectedCheck --stacktrace --info --no-color -q
    - script:
        name: run gradle build
        code: |
          gradle --full-stacktrace --project-cache-dir=$WERCKER_CACHE_DIR build
  after-steps:
    # Use the build results
    - script:
        name: inspect build result
        code: |
          ls -la .
          cp app/build/outputs/apk/*.apk $WERCKER_REPORT_ARTIFACTS_DIR
